import 'package:flutter/material.dart';

class AstrologyView extends StatelessWidget {
  const AstrologyView({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AstrologyView'),
      ),
    );
  }
}

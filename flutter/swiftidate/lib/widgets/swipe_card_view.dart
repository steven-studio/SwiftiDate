import 'package:flutter/material.dart';

class SwipeCardView extends StatelessWidget {
  const SwipeCardView({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SwipeCardView'),
      ),
    );
  }
}
